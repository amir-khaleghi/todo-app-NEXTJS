const DocsPage = ({ params }) => {
  console.log(params);

  return (
    <div className="flex items-center justify-center h-screen text-8xl font-bold">
      Docs
    </div>
  );
};

export default DocsPage;
